# 🎮 Система никнеймов

## Описание функционала

После аутентификации (Email Magic Link или Google OAuth) пользователь должен выбрать уникальный игровой ник. Ник отображается в рейтинге вместо email и **не может быть изменён**.

## Пользовательский сценарий

1. **Вход в игру**
   - Пользователь выбирает способ входа (Email/Google) или играет анонимно
   
2. **Установка никнейма** (для новых пользователей)
   - После первого входа показывается экран установки никнейма
   - Никнейм проверяется на уникальность в реальном времени
   - Если ник занят - показывается ошибка
   - Если свободен - сохраняется в БД
   
3. **Игра**
   - В инструкциях отображается выбранный ник
   - При завершении игры результат сохраняется с никнеймом
   
4. **Рейтинг**
   - В таблице лидеров отображаются никнеймы игроков
   - Текущий игрок подсвечен в списке

## Правила никнейма

- ✅ Длина: **3-20 символов**
- ✅ Символы: **буквы (a-z, а-я), цифры (0-9), пробелы, дефис (-), подчёркивание (_)**
- ✅ Пробелы по краям автоматически удаляются
- ✅ Несколько пробелов подряд запрещены
- ✅ Уникальность: проверка **case-insensitive**
- ❌ Нельзя изменить после установки
- ❌ Другие спецсимволы запрещены

## Структура базы данных

### Таблица `profiles`

```sql
id          BIGSERIAL PRIMARY KEY
user_id     UUID UNIQUE (ссылка на auth.users)
email       TEXT
nickname    TEXT UNIQUE (case-insensitive)
created_at  TIMESTAMP
```

### Таблица `leaderboard` (обновлена)

```sql
id          BIGSERIAL PRIMARY KEY
user_id     UUID (ссылка на auth.users)
nickname    TEXT
time        INTEGER (время в секундах)
created_at  TIMESTAMP
```

## Компоненты

### SetNickname.jsx

Компонент для установки никнейма после первого входа.

**Props:**
- `userId` - ID пользователя из Supabase Auth
- `userEmail` - Email пользователя
- `onNicknameSet(nickname)` - Коллбэк при успешной установке ника

**Особенности:**
- Валидация на клиенте (длина, символы)
- Проверка уникальности в БД
- Красивые анимации и обработка ошибок
- Советы по выбору ника

### App.jsx (изменения)

**Новые состояния:**
```javascript
const [userNickname, setUserNickname] = useState(null)
const [checkingNickname, setCheckingNickname] = useState(false)
```

**Новые функции:**
- `checkUserNickname(userId)` - проверяет наличие ника при входе
- `handleNicknameSet(nickname)` - сохраняет ник в состояние

**Логика:**
1. После входа проверяет наличие ника
2. Если ника нет - показывает `SetNickname`
3. Если ник есть - показывает `Instructions`
4. При сохранении результата использует `nickname` вместо `email`

### Leaderboard.jsx (изменения)

**Props (изменено):**
- `currentUserNickname` вместо `currentUserEmail`

**Особенности:**
- Отображает никнеймы вместо email
- Подсвечивает текущего игрока по нику
- Фоллбэк на "Аноним" если ника нет

### Instructions.jsx (изменения)

**Props (добавлено):**
- `userNickname` - никнейм текущего игрока

**Особенности:**
- Отображает "🎮 Игрок: **nickname**" вместо email
- Стильное отображение с эффектом свечения

## Настройка Supabase

### 1. Выполните SQL миграцию

См. файл `NICKNAME_MIGRATION.md` для подробных инструкций.

Кратко:
```sql
-- Создать таблицу profiles
CREATE TABLE profiles (...);

-- Обновить таблицу leaderboard
ALTER TABLE leaderboard ADD COLUMN user_id UUID;
ALTER TABLE leaderboard ADD COLUMN nickname TEXT;

-- Настроить RLS политики
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Anyone can view profiles" ...
CREATE POLICY "Users can create their own profile" ...
```

### 2. Проверьте RLS политики

В Supabase Dashboard → Authentication → Policies:
- ✅ `profiles`: разрешено чтение всем, создание только своего профиля
- ✅ `leaderboard`: разрешено чтение всем, создание авторизованным

### 3. Обновите переменные окружения

`.env` файл уже настроен с `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`.

## Тестирование

### Сценарий 1: Новый пользователь
1. Войти через Email/Google
2. Увидеть экран установки ника
3. Попробовать ввести короткий ник (< 3 символа) → ошибка
4. Попробовать ввести спецсимволы → ошибка
5. Ввести валидный ник → успех
6. Увидеть экран инструкций с ником

### Сценарий 2: Проверка уникальности
1. Создать первого пользователя с ником "TestUser"
2. Выйти и войти под другим аккаунтом
3. Попробовать взять ник "testuser" (case-insensitive) → ошибка
4. Взять другой ник → успех

### Сценарий 3: Рейтинг
1. Завершить игру несколько раз с разными пользователями
2. Открыть рейтинг
3. Проверить что отображаются ники, а не email
4. Текущий игрок подсвечен

### Сценарий 4: Повторный вход
1. Войти в игру под существующим аккаунтом
2. Экран установки ника НЕ должен показаться
3. Сразу показываются инструкции с сохранённым ником

## Безопасность

- ✅ RLS включен для `profiles` и `leaderboard`
- ✅ Уникальность никнейма проверяется на уровне БД (индекс)
- ✅ Case-insensitive уникальность через `LOWER(nickname)`
- ✅ Пользователь может создать только свой профиль
- ✅ Изменение никнейма запрещено (нет UPDATE политики)

## Ограничения

- Никнейм нельзя изменить после установки
- При удалении пользователя из auth.users профиль удаляется автоматически (CASCADE)
- Анонимные пользователи не могут устанавливать ники

## Возможные улучшения (для будущего)

1. **История имён** - таблица для отслеживания попыток смены ника
2. **Аватары** - добавить поле для аватара пользователя
3. **Статистика** - количество игр, лучший результат
4. **Бейджи/достижения** - система наград
5. **Поиск по нику** - в рейтинге
6. **Профиль игрока** - отдельная страница с подробной статистикой

