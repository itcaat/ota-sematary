# 🔐 Способы аутентификации

## Доступные методы входа

### 1. 🔐 Google OAuth (Рекомендуется)

**Преимущества:**
- ✅ Вход одним кликом
- ✅ Не нужно подтверждать email
- ✅ Безопасно (OAuth 2.0)
- ✅ Удобно для пользователей с Google аккаунтом

**Настройка:**
1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Настройте OAuth 2.0 Client
3. Получите Client ID и Client Secret
4. Добавьте credentials в Supabase
5. Укажите Redirect URI: `https://your-project.supabase.co/auth/v1/callback`

**Как пользователь входит:**
1. Нажимает "Войти через Google"
2. Выбирает Google аккаунт
3. Разрешает доступ → моментально входит

---

### 2. 📧 Email (Magic Link)

**Преимущества:**
- ✅ Не нужен пароль
- ✅ Безопасно
- ✅ Работает с любым email

**Недостатки:**
- ⏳ Нужно проверять почту
- 📬 Может попасть в спам

**Как пользователь входит:**
1. Вводит email
2. Получает письмо со ссылкой
3. Переходит по ссылке → входит

**Настройка email templates:**
```sql
-- В Supabase Dashboard:
Authentication → Email Templates → Customize
```

---

### 3. 👤 Анонимный режим

**Преимущества:**
- ✅ Мгновенный старт игры
- ✅ Не нужна регистрация
- ✅ Идеально для тестирования

**Недостатки:**
- ❌ Результат НЕ сохраняется в рейтинге
- ❌ Нет профиля
- ❌ Нельзя посмотреть статистику

**Как работает:**
1. Пользователь нажимает "Играть без регистрации"
2. Игра начинается сразу
3. При завершении игры результат **не сохраняется** в Supabase
4. Можно вернуться к экрану входа и войти для сохранения результатов

**Технические детали:**
```javascript
// App.jsx
const [isAnonymous, setIsAnonymous] = useState(false)

// Результат не сохраняется для анонимов
if (gameState.gameComplete && session && !isAnonymous) {
  saveScore()
}
```

---

## Сравнение методов

| Функция | Google OAuth | Email | Анонимно |
|---------|--------------|-------|----------|
| Скорость входа | ⚡ Моментально | ⏳ Нужно ждать письмо | ⚡⚡ Мгновенно |
| Сохранение рейтинга | ✅ Да | ✅ Да | ❌ Нет |
| Требует email | ✅ Google account | ✅ Любой email | ❌ Нет |
| Подтверждение | ❌ Не нужно | ✅ Нужно | ❌ Не нужно |
| Безопасность | 🔒 Высокая | 🔒 Высокая | ⚠️ Нет аутентификации |

---

## Рекомендации для пользователей

### Для быстрой игры:
→ **Анонимный режим** 👤

### Для сохранения результата:
→ **Google OAuth** 🔐 (если есть Google аккаунт)  
→ **Email** 📧 (если нет Google)

### Для демо/тестирования:
→ **Анонимный режим** 👤

---

## UI Flow

```
┌─────────────────────────────────┐
│      Экран входа                │
├─────────────────────────────────┤
│                                 │
│  🔐 [Войти через Google]        │
│         ↓                       │
│    OAuth redirect               │
│         ↓                       │
│    ✅ Вход успешен              │
│         ↓                       │
│    Главное меню (с рейтингом)   │
│                                 │
├─────────────────────────────────┤
│                                 │
│  📧 [Email input]               │
│  📧 [Войти через Email]         │
│         ↓                       │
│    Отправлено письмо            │
│         ↓                       │
│    Клик по ссылке               │
│         ↓                       │
│    ✅ Вход успешен              │
│         ↓                       │
│    Главное меню (с рейтингом)   │
│                                 │
├─────────────────────────────────┤
│                                 │
│  👤 [Играть без регистрации]    │
│         ↓                       │
│    ✅ Игра начинается сразу     │
│         ↓                       │
│    Главное меню (БЕЗ рейтинга)  │
│         ↓                       │
│    ⚠️ Результат не сохранится   │
│                                 │
└─────────────────────────────────┘
```

---

## Код компонентов

### Auth.jsx
```jsx
// Три кнопки входа:
<button onClick={handleGoogleLogin}>Google</button>
<button onClick={handleEmailLogin}>Email</button>
<button onClick={handleAnonymousPlay}>Анонимно</button>
```

### App.jsx
```jsx
const [isAnonymous, setIsAnonymous] = useState(false)

// Показываем Auth если не залогинен и не анонимный
if (!session && !isAnonymous) {
  return <Auth onPlayAnonymous={handlePlayAnonymous} />
}

// Не сохраняем результат для анонимов
if (gameComplete && session && !isAnonymous) {
  saveScore()
}
```

---

## Безопасность

### Google OAuth:
- Использует стандарт OAuth 2.0
- Supabase обрабатывает токены
- Пользователь никогда не вводит пароль

### Email:
- Magic Link действует ограниченное время
- Токен одноразовый
- Использует HTTPS

### Анонимный:
- Нет аутентификации
- Нет персональных данных
- Результаты не сохраняются
- Безопасно для тестирования

---

## FAQ

**Q: Можно ли перейти с анонимного на авторизованного?**  
A: Да! Нажмите "Вернуться к входу" в меню

**Q: Сохраняются ли результаты анонимной игры?**  
A: Нет, только локально до перезагрузки страницы

**Q: Нужно ли настраивать Google OAuth?**  
A: Необязательно, но рекомендуется для лучшего UX

**Q: Как удалить аккаунт?**  
A: В Supabase Dashboard → Authentication → Users

**Q: Можно ли добавить другие провайдеры?**  
A: Да! Supabase поддерживает: GitHub, GitLab, Discord, и др.

