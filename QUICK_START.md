# 🚀 Быстрый старт системы рейтинга

## 3 шага для запуска

### 1️⃣ Создайте проект Supabase

1. Зайдите на https://supabase.com
2. Нажмите "New Project"
3. Скопируйте:
   - Project URL
   - Anon/Public Key

### 2️⃣ Создайте таблицу

В SQL Editor Supabase выполните:

```sql
CREATE TABLE leaderboard (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  email TEXT NOT NULL,
  time INTEGER NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL
);

CREATE INDEX idx_leaderboard_time ON leaderboard(time);

ALTER TABLE leaderboard ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Anyone can view leaderboard"
  ON leaderboard FOR SELECT
  USING (true);

CREATE POLICY "Authenticated users can insert scores"
  ON leaderboard FOR INSERT
  WITH CHECK (auth.uid() IS NOT NULL);
```

### 3️⃣ Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

## ✅ Готово!

Запустите проект:

```bash
npm run dev
```

## Как пользоваться

### 🔐 Варианты входа:

**Вариант 1: Google OAuth** (рекомендуется)
1. Нажмите **"Войти через Google"**
2. Выберите Google аккаунт
3. Разрешите доступ

**Вариант 2: Email (Magic Link)**
1. Введите email
2. Получите письмо со ссылкой
3. Перейдите по ссылке

**Вариант 3: Анонимно** (без рейтинга)
1. Нажмите **"Играть без регистрации"**
2. Играйте сразу
3. ⚠️ Результат НЕ сохранится

### 🎮 Игра:

1. **Выберите способ входа** (см. выше)
2. **Нажмите "Начать игру"**
3. **Играйте!**
4. **Завершите игру** → результат сохранится (если не анонимный режим)
5. **Проверьте рейтинг** → увидите свою позицию

## 📧 Настройка Email (опционально)

По умолчанию Supabase отправляет письма со своего домена. Для production:

1. В Supabase: **Authentication** → **Email Templates**
2. Настройте SMTP (SendGrid, Mailgun, и т.д.)
3. Измените шаблоны писем

## 🔐 Настройка Google OAuth (опционально, но рекомендуется)

**Зачем?** Быстрый вход одним кликом без email-подтверждения!

1. Создайте проект в Google Cloud Console
2. Настройте OAuth 2.0 Client
3. Добавьте credentials в Supabase

**Подробная инструкция:** см. раздел "Google OAuth" в `SUPABASE_SETUP.md`

## 🔥 Проблемы?

**Письмо не пришло?**
- Проверьте спам
- Убедитесь что Email provider включён в Supabase
- Проверьте Rate Limiting (Supabase ограничивает письма в dev-режиме)

**Ошибки при сохранении результата?**
- Проверьте RLS политики
- Убедитесь что пользователь аутентифицирован
- Проверьте консоль браузера

**Не видите рейтинг?**
- Убедитесь что таблица создана
- Проверьте политику SELECT
- Откройте консоль и посмотрите ошибки

## 📚 Подробная документация

- `SUPABASE_SETUP.md` - Полная инструкция по настройке
- `RATING_SYSTEM.md` - Техническая документация

