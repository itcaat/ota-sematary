# Рефакторинг BootScene

## Обзор

BootScene.js был отрефакторен из монолитного файла в **1584 строки** в модульную структуру из **7 специализированных модулей** + упрощенная сцена в **50 строк**.

## Новая структура

```
src/game/sprites/
├── SpriteGenerator.js      # Главный класс (24 строки)
├── PlayerSprites.js         # Спрайты игрока (105 строк)
├── ZombieSprites.js         # Спрайты зомби (354 строки)
├── NPCSprites.js            # Спрайты NPC (170 строк)
├── BuildingSprites.js       # Спрайты зданий (437 строк)
├── ItemSprites.js           # Спрайты предметов (335 строк)
└── TileSprites.js           # Тайлы и частицы (107 строк)

src/game/scenes/
├── BootScene.js             # Старая версия (1584 строки)
└── BootScene.refactored.js  # Новая версия (50 строк!)
```

## Разбивка по модулям

### 1. **PlayerSprites.js** - Спрайты игрока
- `player_down`, `player_up`, `player_left`, `player_right`
- Футболка с надписью OTAOPS
- 4 направления движения

### 2. **ZombieSprites.js** - Все враги
- **Обычные зомби**: 4 направления, зелёная кожа, красные глаза
- **Zubkov (босс)**: Больше размером, в костюме, надпись ZUBKOV
- **Zombie Girl (Нарине)**: Худая, в платье, бейджик с именем
- **Crutch**: Летящий костыль

### 3. **NPCSprites.js** - Дружественные персонажи
- **7 NPC**: karpov, rukavkov, mazalov, sergeev, sindov, kozlov, pogozhiy
- Каждый с уникальным цветом рубашки
- **Princess**: Принцесса в платье с короной, 4 направления

### 4. **BuildingSprites.js** - Здания (вид сверху без крыши)
- **Selectel Datacenter**: Тёмный кафель, серверные стойки, кондиционер
- **Yandex Datacenter**: Белая плитка, жёлто-чёрные стойки, рабочее место
- **SALO OFFICE**: Ковролин, open space, переговорка, кофемашина
- **Пхукетск**: Светлый ковролин, 2 команды, кофемашина в центре

### 5. **ItemSprites.js** - Игровые предметы
- **Server**: Серверная стойка с LED индикаторами
- **Server Error**: Сервер с красными LED (ошибка)
- **Medkit**: Белая аптечка с красным крестом
- **Beer**: Кружка пива с пеной и пузырьками
- **Grave Mound**: Холмик земли с травой
- **Cross**: Деревянный крест
- **Tombstone**: Каменное надгробие

### 6. **TileSprites.js** - Тайлы и частицы
- **Grass**: Трава с травинками и деталями
- **Path**: Дорожка с камешками
- **Fence**: Деревянный забор
- **Particle**: Белый квадрат для эффектов
- **Spark**: Круглая искра для фейерверков

### 7. **SpriteGenerator.js** - Главный координатор
Объединяет все модули и управляет порядком генерации.

## Результат рефакторинга

### До:
```javascript
// BootScene.js - 1584 строки
generateAssets() {
  this.generatePlayerSprite()      // ~100 строк
  this.generateZombieSprite()      // ~80 строк
  this.generateZubkovSprite()      // ~120 строк
  this.generateZombieGirlSprite()  // ~90 строк
  this.generateCrutchSprite()      // ~50 строк
  this.generateFriendlyNPCSprite() // ~60 строк
  this.generateServerSprite()      // ~80 строк
  this.generateBuildingSprites()   // ~600 строк
  this.generateMedkitSprite()      // ~40 строк
  this.generateGraveMound()        // ~50 строк
  this.generateGraveyardSprites()  // ~80 строк
  this.generatePrincessSprite()    // ~90 строк
  this.generateBeerSprite()        // ~90 строк
  this.generateTiles()             // ~60 строк
  this.generateParticles()         // ~30 строк
}
```

### После:
```javascript
// BootScene.refactored.js - 50 строк
generateAssets() {
  const spriteGenerator = new SpriteGenerator(this)
  spriteGenerator.generateAll()
}
```

## Преимущества

### 1. **Организация**
- Каждый тип спрайтов в отдельном файле
- Легко найти и изменить конкретный спрайт
- Нет "Ctrl+F по 1584 строкам"

### 2. **Переиспользование**
```javascript
// Теперь можно использовать модули отдельно
import { ZombieSprites } from './sprites/ZombieSprites'

const zombies = new ZombieSprites(scene)
zombies.generateZubkovSprite() // Только босса
```

### 3. **Тестирование**
```javascript
// Легко тестировать каждый модуль
const playerSprites = new PlayerSprites(mockScene)
playerSprites.generate()
expect(mockScene.textures.addCanvas).toHaveBeenCalledWith('player_down', ...)
```

### 4. **Расширение**
Добавить нового врага? Просто добавьте метод в `ZombieSprites.js`:
```javascript
generateNewEnemySprite() {
  // Ваш код здесь
}
```

## Как мигрировать

### Вариант 1: Полная миграция (рекомендуется)

```bash
# 1. Бэкап старой версии
mv src/game/scenes/BootScene.js src/game/scenes/BootScene.old.js

# 2. Используйте новую версию
mv src/game/scenes/BootScene.refactored.js src/game/scenes/BootScene.js

# 3. Тестируйте игру
npm start

# 4. Если все ОК, удалите старую версию
rm src/game/scenes/BootScene.old.js
```

### Вариант 2: Постепенная миграция

Импортируйте нужную версию:
```javascript
// В src/components/Game.jsx
import BootScene from '../game/scenes/BootScene.refactored'
```

### Вариант 3: A/B тестирование

Сравните обе версии:
```javascript
const config = {
  scene: [
    process.env.USE_NEW_BOOTSCENE 
      ? BootSceneRefactored 
      : BootSceneOld,
    MainScene
  ]
}
```

## Пример использования модулей

### Генерация только зданий
```javascript
import { BuildingSprites } from './sprites/BuildingSprites'

class MyCustomScene extends Phaser.Scene {
  preload() {
    const buildings = new BuildingSprites(this)
    buildings.generate() // Генерирует все 4 здания
  }
}
```

### Генерация только одного здания
```javascript
import { BuildingSprites } from './sprites/BuildingSprites'

const buildings = new BuildingSprites(this)
buildings.createSelectelBuilding()  // Только Selectel
buildings.createYandexBuilding()    // Только Yandex
```

### Кастомизация спрайтов
Теперь легко изменить спрайт - просто отредактируйте нужный метод в нужном модуле:

```javascript
// В src/game/sprites/PlayerSprites.js
drawTopDownPlayer(ctx, direction) {
  // Измените цвет футболки
  ctx.fillStyle = '#ff0000' // Красная вместо белой
  ctx.fillRect(8, 10, 16, 14)
  
  // Измените надпись
  ctx.fillText('CUSTOM', 7, 20) // Вместо OTAOPS
}
```

## Добавление нового спрайта

### Пример: Добавить нового врага

1. **Откройте `ZombieSprites.js`**
```javascript
generateNewEnemySprite() {
  const canvas = document.createElement('canvas')
  canvas.width = 32
  canvas.height = 32
  const ctx = canvas.getContext('2d')
  ctx.imageSmoothingEnabled = false
  
  // Рисуем врага
  ctx.fillStyle = '#purple'
  ctx.fillRect(0, 0, 32, 32)
  
  this.scene.textures.addCanvas('new_enemy', canvas)
}
```

2. **Добавьте вызов в `generate()`**
```javascript
generate() {
  this.generateZombieSprite()
  this.generateZubkovSprite()
  this.generateZombieGirlSprite()
  this.generateCrutchSprite()
  this.generateNewEnemySprite() // ← Добавили
}
```

Готово! Новый враг генерируется автоматически.

## Сравнение производительности

Рефакторинг **не влияет** на производительность:
- Спрайты генерируются один раз при загрузке
- Нет дополнительных вызовов функций в рантайме
- Память используется так же эффективно

## Файловая статистика

| Модуль | Строк кода | Отвечает за |
|--------|------------|-------------|
| `PlayerSprites.js` | 105 | Игрок, 4 направления |
| `ZombieSprites.js` | 354 | Все враги (зомби, Zubkov, девочка, костыль) |
| `NPCSprites.js` | 170 | 7 NPC + принцесса |
| `BuildingSprites.js` | 437 | 4 здания (Selectel, Yandex, офисы) |
| `ItemSprites.js` | 335 | Предметы (пиво, аптечка, серверы, могилы) |
| `TileSprites.js` | 107 | Тайлы (трава, дорожка, забор, частицы) |
| `SpriteGenerator.js` | 24 | Координатор всех модулей |
| `BootScene.refactored.js` | 50 | Новая сцена загрузки |
| **Итого** | **1582** | **Примерно столько же, но модульно!** |

## Заключение

Рефакторинг BootScene превратил один гигантский файл в **чистую модульную архитектуру**:

✅ **Проще читать** - каждый модуль отвечает за одно  
✅ **Проще изменять** - найти и изменить спрайт за секунды  
✅ **Проще тестировать** - можно тестировать модули по отдельности  
✅ **Проще расширять** - добавлять новые спрайты легко  
✅ **Та же производительность** - никаких потерь!

Теперь весь код игры организован в логичную структуру:
- **entities/** - игровые сущности
- **systems/** - игровые системы
- **sprites/** - генераторы спрайтов ← только что добавили!
- **scenes/** - Phaser сцены (упрощенные)

Игра готова к дальнейшей разработке! 🚀

