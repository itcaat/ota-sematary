# Рефакторинг NPC системы

## Проблема

Было 4 отдельных класса с практически идентичной логикой:
- `Zombie.js` (243 строки)
- `Zubkov.js` (147 строк)
- `ZombieGirl.js` (192 строки)
- `FriendlyNPC.js` (190 строк)

**Итого: 772 строки дублированного кода** 😰

## Решение

Создана **единая система** на основе конфигураций:

### 1. **BaseNPC.js** (250 строк)
Базовый класс с общей логикой для всех NPC:
- AI поведение (патрулирование, преследование, возврат)
- Система фраз с анимацией
- Поддержка разных режимов патрулирования
- Визуальные эффекты (тинт, покачивание)

### 2. **NPCConfigs.js** (160 строк)
Конфигурации всех NPC в одном месте:
- `zombieConfig` - обычные зомби
- `zubkovConfig` - босс Zubkov
- `zombieGirlConfig` - зомби-девочка Нарине
- `friendlyNPCConfigs` - 7 дружественных NPC

### 3. **NPCManager.js** (155 строк)
Единый менеджер всех NPC:
- Создание всех типов NPC
- Обновление всех NPC
- Управление костылями
- API для получения групп для коллизий

**Итого: 565 строк вместо 772!** ✨

## Преимущества

### ✅ Data-Driven подход
Теперь добавить нового NPC = добавить конфиг:

```javascript
export const newEnemyConfig = {
  type: 'new_enemy',
  spriteKey: 'new_enemy',
  hasDirections: true,
  hostile: true,
  showName: true,
  name: 'NEW BOSS',
  nameColor: '#00ff00',
  phrases: ['Я новый босс!'],
  phraseColor: '#00ff00',
  ai: {
    speed: 100,
    chaseSpeed: 200,
    detectionRange: 300,
    loseRange: 400
  }
}
```

### ✅ Нет дублирования кода
Вся логика в одном месте:
- Изменили AI → работает для всех
- Добавили фичу → доступна всем
- Нашли баг → исправили раз и навсегда

### ✅ Гибкие конфигурации
Каждый NPC полностью настраиваемый:
- Скорость, дальность обнаружения
- Режим патрулирования (по X, Y или точкам)
- Фразы и их цвет
- Визуальные эффекты
- Враждебность к игроку

### ✅ Легко расширять
Хотите добавить новую механику?
```javascript
// В BaseNPC.js добавьте новую опцию
if (this.config.teleport) {
  this.setupTeleport()
}

// В конфиге включите её
export const teleportingZombieConfig = {
  ...zombieConfig,
  teleport: true,
  teleportRange: 500
}
```

## Сравнение до/после

### До рефакторинга:

```javascript
// Zombie.js - 243 строки
export class ZombieManager {
  constructor(scene) { /* ... */ }
  create() { /* дублированная логика */ }
  showPhrase(zombie) { /* дублированная логика */ }
  throwCrutch() { /* уникальная логика */ }
  update() { /* дублированная логика AI */ }
}

// Zubkov.js - 147 строк
export class ZubkovEntity {
  constructor(scene) { /* ... */ }
  create(x, y) { /* дублированная логика */ }
  showAlert() { /* дублированная логика */ }
  update() { /* дублированная логика AI */ }
}

// ZombieGirl.js - 192 строки
export class ZombieGirlEntity {
  constructor(scene) { /* ... */ }
  create(x, y) { /* дублированная логика */ }
  showPhrase() { /* дублированная логика */ }
  update() { /* дублированная логика AI */ }
}

// FriendlyNPC.js - 190 строк
export class FriendlyNPCManager {
  constructor(scene) { /* ... */ }
  create() { /* дублированная логика */ }
  showPhrase(npc) { /* дублированная логика */ }
  update() { /* дублированная логика */ }
}
```

### После рефакторинга:

```javascript
// BaseNPC.js - 250 строк (вся общая логика)
export class BaseNPC {
  constructor(scene, config) { /* универсальный конструктор */ }
  create(x, y) { /* универсальное создание */ }
  showPhrase() { /* универсальные фразы */ }
  update() { /* универсальный AI */ }
  updateAI(data) { /* универсальное поведение */ }
}

// NPCConfigs.js - 160 строк (только данные!)
export const zombieConfig = { /* параметры зомби */ }
export const zubkovConfig = { /* параметры Zubkov */ }
export const zombieGirlConfig = { /* параметры девочки */ }
export const friendlyNPCConfigs = { /* параметры NPC */ }

// NPCManager.js - 155 строк (создание и управление)
export class NPCManager {
  createZombies() { /* создаёт с zombieConfig */ }
  createZubkov() { /* создаёт с zubkovConfig */ }
  createZombieGirl() { /* создаёт с zombieGirlConfig */ }
  createFriendlyNPCs() { /* создаёт с friendlyNPCConfigs */ }
  update() { /* обновляет всех */ }
}
```

## Как работает

### 1. Конфиг определяет поведение

```javascript
const config = {
  // Базовые параметры
  type: 'zombie',
  spriteKey: 'zombie',
  hasDirections: true,  // Есть спрайты для 4 направлений
  hostile: true,        // Враждебен к игроку
  
  // Визуал
  showName: false,
  nameColor: '#ffffff',
  chaseTint: 0xff6666,  // Красный при преследовании
  
  // Фразы
  phrases: ['Фраза 1', 'Фраза 2'],
  phraseColor: '#ff4444',
  phraseInterval: 5000,
  
  // AI
  ai: {
    speed: 60,
    chaseSpeed: 120,
    detectionRange: 150,
    loseRange: 250,
    patrolX: [100, 300]  // Патрулирует по X от 100 до 300
  }
}
```

### 2. BaseNPC читает конфиг

```javascript
const npc = new BaseNPC(scene, config)
npc.create(x, y)

// Внутри create():
if (config.hostile) {
  // Настраивает враждебное поведение
}
if (config.phrases) {
  // Запускает таймер фраз
}
if (config.ai.patrolX) {
  // Настраивает патрулирование по X
}
```

### 3. NPCManager управляет всеми

```javascript
const manager = new NPCManager(scene)
manager.create()

// Создаёт:
// - 6 обычных зомби с zombieConfig
// - 1 Zubkov с zubkovConfig
// - 1 девочку с zombieGirlConfig
// - 7 друзей с friendlyNPCConfigs

manager.update()
// Обновляет всех NPC за один вызов
```

## Примеры использования

### Добавить нового босса

```javascript
// В NPCConfigs.js
export const superBossConfig = {
  ...zubkovConfig,  // Наследуем от Zubkov
  name: 'SUPER BOSS',
  scale: 1.5,
  ai: {
    ...zubkovConfig.ai,
    speed: 100,
    chaseSpeed: 200
  },
  phrases: ['Я супер-босс!', 'Ты обречён!']
}

// В NPCManager.js
createSuperBoss() {
  this.superBoss = new BaseNPC(this.scene, superBossConfig)
  this.superBoss.create(1000, 1000)
}
```

### Изменить поведение всех зомби

```javascript
// В NPCConfigs.js
export const zombieConfig = {
  // ...
  ai: {
    speed: 80,        // Было 60 - стали быстрее!
    chaseSpeed: 150,  // Было 120
    // ...
  }
}
// Всё! Все зомби теперь быстрее
```

### Добавить новую механику

```javascript
// В BaseNPC.js
updateAI(data) {
  // Существующий код...
  
  // Новая механика: телепортация
  if (this.config.canTeleport && data.state === 'chase') {
    const dist = Phaser.Math.Distance.Between(
      this.sprite.x, this.sprite.y,
      this.scene.player.x, this.scene.player.y
    )
    
    if (dist > 500) {
      // Телепортируемся ближе к игроку
      this.sprite.x = this.scene.player.x + 100
      this.sprite.y = this.scene.player.y
    }
  }
}

// В NPCConfigs.js
export const teleportZombieConfig = {
  ...zombieConfig,
  canTeleport: true
}
```

## Миграция

Старые файлы можно удалить:
```bash
rm src/game/entities/Zombie.js
rm src/game/entities/Zubkov.js
rm src/game/entities/ZombieGirl.js
rm src/game/entities/FriendlyNPC.js
```

Новые файлы уже интегрированы:
- ✅ `BaseNPC.js` - базовый класс
- ✅ `NPCConfigs.js` - конфигурации
- ✅ `NPCManager.js` - менеджер
- ✅ `MainScene.js` - обновлён для использования NPCManager
- ✅ `CollisionSystem.js` - обновлён для работы с NPCManager

## Результат

### До:
- 4 файла, 772 строки
- Дублирование кода
- Сложно добавлять NPC
- Сложно изменять поведение

### После:
- 3 файла, 565 строк (-207 строк, -27%)
- Нет дублирования
- Добавить NPC = добавить конфиг (5 строк)
- Изменить всех = изменить BaseNPC

**Код стал проще, гибче и мощнее!** 🚀

## Архитектурный паттерн

Использован **Entity-Component-System (ECS)** подход:
- **Entity**: BaseNPC (универсальная сущность)
- **Component**: Config (данные и поведение)
- **System**: NPCManager (управление всеми сущностями)

Это профессиональный паттерн, используемый в:
- Unity (GameObject + Components)
- Unreal Engine (Actor + Components)
- Three.js (Object3D + Components)
- Многих AAA играх

Теперь ваш код следует индустриальным стандартам! 🎮✨

